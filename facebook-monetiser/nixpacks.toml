[phases.build]
cmds = ["npm install", "npm run build"]

[phases.start]
cmd = "npm start"

[start]
cmd = "npm start"

[static]
# Configure data directory as a mount point for persistence
dir = "/data"
